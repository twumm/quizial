extends layout
block content
  
  h4= title
  br
  p= user.username
  p= user.email
  p= user.questionsPosted

  a(href='/quiz/user/changepassword') Change password
  br
  if userLoggedIn
    a(href='/quiz/user/logout') Logout
  